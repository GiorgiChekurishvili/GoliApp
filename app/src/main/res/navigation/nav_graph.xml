<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.goliapp.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_home_to_matchDetail"
            app:destination="@id/matchDetailFragment" />
    </fragment>

    <fragment
        android:id="@+id/standingsFragment"
        android:name="com.example.goliapp.ui.standings.StandingsFragment"
        android:label="Table"
        tools:layout="@layout/fragment_standings">
        <argument
            android:name="leagueId"
            app:argType="integer"
            android:defaultValue="39"/>
    </fragment>

    <fragment
        android:id="@+id/favouritesFragment"
        android:name="com.example.goliapp.ui.favourites.FavouritesFragment"
        android:label="Favourites"
        tools:layout="@layout/fragment_favourites">
        <action
            android:id="@+id/action_favourites_to_matchDetail"
            app:destination="@id/matchDetailFragment" />
    </fragment>

    <fragment
        android:id="@+id/matchDetailFragment"
        android:name="com.example.goliapp.ui.matches.MatchDetailFragment"
        android:label="Match Details"
        tools:layout="@layout/fragment_match_detail">
        <argument
            android:name="matchId"
            app:argType="integer" />
    </fragment>

</navigation>